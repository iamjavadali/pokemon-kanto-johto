// Compatibility include retained so the existing C translation unit does not need
// unrelated edits. scaninc recognizes #include inside .inc files and follows this
// header, where every INCGFX dependency is declared directly with literal paths.
#include "golden_yellow_pikapic_assets.h"
